# Adaptador para Claude Code en deep-diffusion

Lee y aplica [`AGENTS.md`](AGENTS.md). Las reglas del proyecto, su estado y la
documentación canónica no se duplican aquí.

Para cada tarea, selecciona el rol correspondiente en `docs/ai/agents/`, lee el
workflow de `workflows/` y entrega evidencia de las validaciones ejecutadas.
